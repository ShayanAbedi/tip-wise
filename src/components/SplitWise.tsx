import MainBox from './Main/MainBox';
import Title from './Title';

type Props = {};

const SplitWise = (props: Props) => {
  return (
    <div>
      <Title />
      <MainBox />
    </div>
  );
};

export default SplitWise;
